<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- הרשאות לאינטרנט ולגישה למיקום -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/app_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/app_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.GeoAppFinal"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- הגדרת מפתח API של Google Maps -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAG9CpKVU-swklv9tET_3SAHEMV2Da9EHg" />

        <!-- פעילות מסך ההתחלה -->
        <activity
            android:name=".StartMenuActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- פעילות ראשית -->
        <activity android:name=".GeoFunctionHelper" android:exported="true" />

        <!-- הוספת CountriesActivity -->
        <activity android:name=".CountriesActivity" />

        <!-- פעילות דף המסעדות בישראל -->
        <activity android:name=".NorthIsraelRestaurantPage" android:exported="true" />

        <!-- פעילות דף המסעדות בישראל -->
        <activity android:name=".CenterIsraelRestaurantPage" android:exported="true" />

        <!-- פעילות דף המסעדות בישראל -->
        <activity android:name=".SouthIsraelRestaurantPage" android:exported="true" />

        <!-- פעילות דף המסעדות בלבנון -->
        <activity android:name=".LebanonRestaurantPage" android:exported="true" />

    </application>
</manifest>
